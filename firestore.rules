rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras temporárias para desenvolvimento - ALTERAR EM PRODUÇÃO
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 6, 1);
    }
  }
} 